# Adapter（适配器模式）

#### 定义
**适配器模式**是一种 `结构性` 设计模式，它能使接口不兼容的对象能够互相合作。


#### 💡适用应用场景
当你希望使用某个类，但是其接口与其他代码不兼容时，可以使用适配器类

#### 优缺点
✅ 单一职责原则你可以将接口或数据转换代码从程序主要业务逻辑中分离
✅ 开笔原则，只要客户端代码通过客户端接口与适配器进行交互，你就能在不修改现有客户端代码的情况下在程序中添加新类型的适配器

❌ 代码整体复杂度增加，因为你需要新增一系列接口和类。有时直接更改服务类使其与其他代码兼容会更简单。

